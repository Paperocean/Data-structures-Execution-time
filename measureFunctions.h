#ifndef MEASURE_FUNCTIONS_H
#define MEASURE_FUNCTIONS_H

auto pushTimeMeasureMallocArray(int value);
auto pushTimeMeasureArray(int value);
auto pushTimeMeasureVector(int value);

#endif // !MEASURE_FUNCTIONS_H
